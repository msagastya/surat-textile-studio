Header from surat-textile-studio. Use via `window.SuratTextileStudio.Header` (bundle loaded from the root `_ds_bundle.js`).

## Examples

### DefaultHeader

```jsx
() => (
  <div style={{ background: "#080706", width: "100%" }}>
    <Header
      designName="Bridal Banarasi Jaal"
      setDesignName={() => {}}
      fabricType="Jacquard"
      setFabricType={() => {}}
      zariType="Real Zari"
      setZariType={() => {}}
      targetMarket="Sahara Darwaja"
      setTargetMarket={() => {}}
    />
  </div>
)
```

### GeorgetteDesign

```jsx
() => (
  <div style={{ background: "#080706", width: "100%" }}>
    <Header
      designName="Party Wear Georgette"
      setDesignName={() => {}}
      fabricType="Georgette"
      setFabricType={() => {}}
      zariType="Tested Zari"
      setZariType={() => {}}
      targetMarket="Ring Road"
      setTargetMarket={() => {}}
    />
  </div>
)
```
